'use strict';
// NASim presentation is separate so existing walker reports retain their meaning.
function nasimNotice() {return '<div class="notice">Partial diagnostic profile · four shared components, covering 50% of the original score weights. The available weights are normalized. No calibrated NASim thresholds apply, and this index cannot be compared with BipedalWalker scores.</div>';}
function nasimStats(items) {return `<div class="stats">${items.map(([title,value,foot])=>`<div class="stat"><div class="stat-label">${esc(title)}</div><div class="stat-value">${esc(value)}</div><div class="stat-foot">${esc(foot)}</div></div>`).join('')}</div>`;}
function renderNasimOverview() {
  const r=report.graph_ppo,e=experiment();
  let html=heading('Follow the reward. Inspect the behavior.', 'Graph PPO in a simulated pentesting environment. One policy, held-out networks, traceable evidence.');
  if(typeof r?.hackability_score!=='number') {
    $('#content').innerHTML=html+`<div class="status-strip"><div class="strip-left"><i class="small-dot"></i><span>NASim adapter available</span></div><span class="mono">SINGLE AGENT / GRAPH PPO</span></div><div class="split-grid"><section class="panel"><div class="panel-head"><h2 class="panel-title">Your next controlled experiment.</h2></div><div class="panel-body"><ol class="method-list"><li>Select Graph PPO and a recorded reward profile.</li><li>Start training when you are ready. Checkpoints stay in a new experiment directory.</li><li>Choose Evaluate completed to run 60 episodes on 20 held-out networks.</li><li>Inspect the shared diagnostics, reward components and individual actions.</li></ol></div></section><section class="panel"><div class="panel-head"><h2 class="panel-title">Evidence starts after training.</h2></div><div class="panel-body"><p class="muted">${e?'This experiment has no completed trained-policy report yet. Follow its status in Experiments & jobs.':'No NASim training or trained-policy evaluation has been launched by this setup. Inference checks do not count as a trained report.'}</p><p class="note">The current notebook reward is available. The scan-penalty alternative is explicitly a reconstruction; it is not a verified historical reward.</p><button class="button primary" data-action="${e?'runs':'launch'}">${e?'View experiment status':'Configure Graph PPO'}</button></div></section></div>`+nasimNotice();
    return;
  }
  const d=r.domain_diagnostics||{},c=r.context||{};
  html+=`<div class="status-strip"><span>Trained-policy report / ${esc(e?.name||current)}</span><span class="mono">${esc(e?.profile||r.reward_profile)}</span></div>`;
  html+=nasimStats([['SHARED DIAGNOSTIC INDEX',fmt(r.hackability_score),'Four applicable components'],['GOAL SUCCESS',`${fmt(c.goal_success_rate*100,1)}%`,`${num(c.completed_episodes)} held-out episodes`],['REWARDED / NO PROGRESS',num(d.positive_reward_without_progress_steps),'Context flag · requires inspection'],['FAILED / POSITIVE REWARD',num(d.positive_reward_on_failed_action_steps),'Context flag · requires inspection']]);
  html+=nasimNotice()+`<div class="split-grid"><section class="panel"><div class="panel-head"><h2 class="panel-title">The learned policy.</h2><span class="panel-kicker">GRAPH PPO</span></div><div class="panel-body">${barChart(report)}<p class="note">The index measures critic and reward-stream diagnostics. A low index does not rule out a consistently learned reward exploit.</p></div></section><section class="panel"><div class="panel-head"><h2 class="panel-title">What deserves a closer look?</h2></div><div class="panel-body kv-grid">${kv('Honeypot targeting steps',num(d.honeypot_targeted_steps))}${kv('Consecutive repeated actions',num(d.consecutive_repeated_action_steps))}${kv('Native reward total',fmt(r.reward_audit?.native_return_total,2))}${kv('Training reward total',fmt(r.reward_audit?.extrinsic_return_total,2))}${kv('Recorded evaluation steps',num(r.reward_audit?.steps))}${kv('Checkpoint training steps',num(r.checkpoint_steps))}</div><div class="panel-body"><button class="button secondary" data-action="timeline">Inspect event timeline</button></div></section></div>`+agentTable();
  $('#content').innerHTML=html;
}
function renderNasimAgents() {
  const r=report.graph_ppo;selectedAgent='graph_ppo';
  let html=heading('Every component, in context.', 'The notebook policy, measured through the existing shared metric functions.');
  if(typeof r?.hackability_score!=='number'){$('#content').innerHTML=html+empty('No trained Graph PPO report yet.','Train an experiment, then evaluate its completed checkpoint.','runs','View experiments');return;}
  const d=r.domain_diagnostics||{},profile=r.score_profile;
  html+=`<div class="agent-detail-header"><div><h2>Graph PPO</h2><p class="muted">${esc(r.verdict)}</p></div><div class="agent-detail-score">${fmt(r.hackability_score)} <small>/ 1.000</small></div></div>`+nasimNotice();
  html+=`<div class="split-grid"><section class="panel"><div class="panel-head"><h3>Shared score components</h3><span class="panel-kicker">4 DIAGNOSTICS</span></div><div class="panel-body bars-list">${Object.entries(profile.weights).map(([k,w])=>`<div class="bar-row"><div class="bar-label">${esc(metricNames[k])}<small>${fmt(w*100,0)}% WEIGHT · ${fmt(w*r.sub_scores[k])} CONTRIBUTION</small></div><div class="metric-track"><span style="width:${r.sub_scores[k]*100}%"></span></div><span class="bar-value">${fmt(r.sub_scores[k])}</span></div>`).join('')}</div></section><section class="panel"><div class="panel-head"><h3>Reward components</h3><span class="panel-kicker">SUM OVER EVALUATION</span></div><div class="panel-body kv-grid">${Object.entries(d.reward_component_totals||{}).map(([k,v])=>kv(human(k),fmt(v,2))).join('')}</div></section></div>`;
  html+=`<div class="split-grid"><section class="panel"><div class="panel-head"><h3>Domain evidence</h3></div><div class="panel-body kv-grid">${Object.entries(d).filter(([,v])=>typeof v==='number').map(([k,v])=>kv(human(k),fmt(v,k.includes('entropy')?3:0))).join('')}</div><div class="panel-body"><p class="note">${esc(d.description)}</p></div></section><section class="panel"><div class="panel-head"><h3>Unavailable diagnostics</h3></div><div class="panel-body">${Object.entries(profile.unsupported).map(([k,v])=>`<div class="finding"><div><strong>${esc(metricNames[k]||human(k))}</strong><p>${esc(v)}</p></div></div>`).join('')}</div></section></div>`;
  html+=`<div class="section-heading"><h2>Held-out episode results</h2><span class="muted">Three transition seeds per network</span></div><div class="panel table-wrap"><table><thead><tr><th>SCENARIO / SEED</th><th>STEPS</th><th>REWARD</th><th>NATIVE REWARD</th><th>GOAL</th><th>BOUNDARY</th></tr></thead><tbody>${(r.episode_results||[]).map(e=>`<tr><td>${esc(e.scenario)}<div class="agent-desc">${esc(e.seed)}</div></td><td>${num(e.length)}</td><td>${fmt(e.reward,2)}</td><td>${fmt(e.native_reward,2)}</td><td>${e.goal_success?'Reached':'Not reached'}</td><td>${esc(e.boundary_reason)}</td></tr>`).join('')}</tbody></table></div><details class="raw-details"><summary>Inspect raw metrics and provenance</summary><pre>${esc(JSON.stringify({...r,timeline:{method:r.timeline.method,recorded_steps:r.timeline.steps.length}},null,2))}</pre></details>`;
  $('#content').innerHTML=html;
}
function nasimStepValues(r) {
  return kv('Episode / step',`${r.episode} / ${r.episode_step}`)+kv('Scenario',r.scenario)+kv('Action / target',`${r.action} / (${r.target.join(', ')})`)+kv('Outcome',r.outcome)+kv('Training reward',fmt(r.reward,4))+kv('Native reward',fmt(r.native_reward,4))+kv('Critic estimate',fmt(r.value,4))+kv('TD residual',fmt(r.td_residual,4))+kv('Priority signal',fmt(r.signal))+kv('Valid-action entropy',fmt(r.normalized_entropy))+kv('Valid actions',num(r.valid_actions))+kv('Episode boundary',r.boundary_reason||'None')+kv('Progress',Object.entries(r.progress).map(([k,v])=>`${k}: ${v>0?'+':''}${v}`).join(' / '))+kv('Reward components',Object.entries(r.reward_components).map(([k,v])=>`${human(k)}: ${fmt(v,2)}`).join(' / '))+kv('Context flags',(r.flags||[]).join(' / ')||'None recorded');
}
function nasimComparison(a,b) {
  const x=a.graph_ppo,y=b.graph_ppo;if(!x||!y)return '';
  const fields=[['Goal success rate',x.context.goal_success_rate,y.context.goal_success_rate],['Mean training reward per step',x.context.reward_rate_mean,y.context.reward_rate_mean],...['positive_reward_without_progress_steps','positive_reward_on_failed_action_steps','honeypot_targeted_steps'].map(k=>[human(k),x.domain_diagnostics[k],y.domain_diagnostics[k]])];
  return `<div class="section-heading"><h2>Behavior alongside the score</h2></div><div class="panel table-wrap"><table><thead><tr><th>OBSERVATION</th><th>BASELINE</th><th>COMPARISON</th></tr></thead><tbody>${fields.map(([k,v,w])=>`<tr><td>${esc(k)}</td><td>${fmt(v)}</td><td>${fmt(w)}</td></tr>`).join('')}</tbody></table></div><p class="note">Native returns change when scan costs change. The reconstruction also removes shaping, so it is not a scan-cost-only ablation. Use matched budgets and seeds; neither reward profile is assumed hack-free.</p>`;
}
function renderNasimMethod() {
  $('#content').innerHTML=heading('Same diagnostics. An explicit adapter.', 'Graph observations and discrete commands need an environment-aware collection layer.',false)+nasimNotice()+`<div class="split-grid"><section class="panel"><div class="panel-head"><h3>What is preserved</h3></div><div class="panel-body"><ol class="method-list"><li>The original notebook stays untouched. Model, reward and PPO definitions live in an import-safe copy.</li><li>Frozen graph embeddings are replayed through the policy’s own trained critic.</li><li>TD error, calibration gap, gap variance and reward concentration call the existing metric functions unchanged.</li><li>Domain flags record progress, failed-action rewards and honeypot targeting separately from the index.</li><li>20 held-out networks, three transition seeds each; timelines describe evaluation steps.</li></ol></div></section><section class="panel"><div class="panel-head"><h3>Runtime & reproducibility</h3></div><div class="panel-body"><span class="field-label">NASIM WORKER PYTHON</span><code class="code-inline">${esc(state.nasim_python)}</code><p class="note">An optional isolated runtime is used in this repository. An installed package uses its current Python environment; install <code>fallax-toolkit[nasim]</code>.</p><p class="muted">Each checkpoint records its reward parameters, source hash, network fingerprint, training steps and dependency versions. Evaluation requires a completed, matching checkpoint.</p><p class="note">Available runtime path is not a full dependency check. Run <code>python -m fallax_toolkit.nasim.runner preflight</code> in the NASim runtime to verify inference with zero optimizer steps.</p></div></section></div><section class="panel"><div class="panel-head"><h3>Interpretation limits</h3></div><div class="panel-body"><ul class="method-list">${(report._limitations||['No trained-policy report exists until you train and evaluate.','A low critic error can coexist with a consistently learned exploit.','Graph topology and some notebook trackers use simulator information; information leakage can affect results.','No-valid-action states end as explicit truncations. Shared metrics and copied GAE use zero bootstrap at boundaries.','The earlier scan-penalty profile is a reconstruction; its historical reward has not been verified.','Continuous-action saturation, graph perturbation tests and cross-agent distance are unavailable.']).map(t=>`<li>${esc(t)}</li>`).join('')}</ul></div></section>`;
}
